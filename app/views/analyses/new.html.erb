<div class="min-h-screen bg-gray-50 flex items-center justify-center">
  <div class="bg-white p-8 rounded-xl shadow-md w-full max-w-lg">
    <div class="flex justify-between items-center mb-2">
      <h1 class="text-2xl font-bold text-gray-900">YouTube Feedback Analyzer</h1>
      <%= link_to "History →", analyses_path, class: "text-sm text-red-600 hover:underline" %>
    </div>
    <p class="text-gray-500 mb-6">Paste a YouTube video URL to analyze its comments with AI.</p>

    <%= form_with model: @analysis do |f| %>
      <% if @analysis.errors.any? %>
        <div class="bg-red-50 text-red-700 p-3 rounded mb-4">
          <%= @analysis.errors.full_messages.first %>
        </div>
      <% end %>

      <div class="mb-4">
        <%= f.label :video_url, "Video URL", class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.text_field :video_url, placeholder: "https://www.youtube.com/watch?v=...",
            class: "w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-red-500" %>
      </div>

      <%= f.submit "Analyze comments →",
          class: "w-full bg-red-600 hover:bg-red-700 text-white font-semibold py-2 px-4 rounded-lg cursor-pointer",
          data: { turbo_submits_with: "Analyzing..." } %>
    <% end %>
  </div>
</div>
